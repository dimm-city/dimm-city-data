'use strict';

/**
 * character-release service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::character-release.character-release');
